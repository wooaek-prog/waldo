# 수광점 자료 근거 등급 — 도면 실측분과 추정분

수광점 1,527개가 모두 같은 근거로 만들어진 것이 아니다. **865개(57%)는 도면
없이 추정한 것**이라 QGIS 에서 따로 확인할 수 있게 레이어를 나눴다.

```bash
python3 school_receptor_provenance.py --buildings <AL_D010.gpkg>
# → outputs/school_compliance/qgis_provenance/
```

## 등급 정의

| 등급 | 뜻 | 수광점 |
|---|---|---:|
| **A** 도면기준 | 사용자가 준 정면도·분석지점도에서 층고와 창 높이를 읽었다 | 441 |
| **A−** 도면기준(추론·계측 포함) | 도면은 있으나 **폴리곤 대응·제원을 추론**했거나 **래스터를 계측**했다 | 221 |
| **B** 일반식 | 도면을 못 받아 **층고·창높이·수평배치가 전부 가정값**이다 | 341 |
| **C+** 운동장 격자 도면 | **칸 수·방위는 분석지점도대로**, 경계 위치·크기만 추정 | 0 |
| **C** 운동장 근사 | 분석지점도도 없어 **옥외지반을 8m 격자로 근사**했다 | 524 |
| | **추정분(B+C)** | **865 (57%)** |

## 동별

| 학교 | 구분 | 등급 | 수광점 | 근거 |
|---|---|---|---:|---|
| 여의도고 | ①(4층) | **B** | 40 | 도면 없음 |
| 여의도고 | ②(4층) | **B** | 176 | 도면 없음 |
| 여의도고 | ③(3층) | **B** | 30 | 도면 없음 |
| 여의도고 | 운동장 | **C** | 383 | 옥외지반 근사 |
| 여의도중 | 본관(5층) | A | 153 | 본관 정면도 + 배면도 레벨치수 |
| 여의도중 | 체육관동 | A | 88 | 체육관동 4면도 |
| 여의도중 | 체육관동 부속(원형부) | **A−** | 48 | 4면도(폴리곤 대응은 추론) |
| 여의도중 | 운동장 | **A−** | **76** | **옥외체육장 분석지점도 6×13 + 배치도 경계** |
| 여의도여고 | 본관동 저층부(4층) | A | 16 | 본관동 분석지점도 |
| 여의도여고 | 본관동 고층부(5층) | A | 50 | 본관동 분석지점도 |
| 여의도여고 | A동(3층) | **A−** | 33 | A동 수광지침서 |
| 여의도여고 | ①(5층) | **B** | 30 | 도면 없음 |
| 여의도여고 | ②(5층) | **B** | 65 | 도면 없음 |
| 여의도여고 | 운동장 | **C** | 141 | 옥외지반 근사 |
| 여의도초 | 본관(4층) | A | 64 | 본관 정면도 |
| 여의도초 | 별관(5층) | A | 70 | 별관 정면도 |
| 여의도초 | 운동장 | **A−** | **64** | **옥외체육장 분석지점도 8×8 + 배치도 경계** |

## 무엇을 추정했나

### B 일반식 (341개) — 여의도고 전체 교사동, 여의도여고 ①·②동

도면을 못 받았다. 층고 **3.5m**, 창중심 **바닥 +1.2m**, 남향계열 외벽 전체를
**5m 등간격**으로 분할 — 층고·창높이·수평배치가 전부 가정값이다. 도면을 받으면
**위치와 높이가 모두 바뀐다.** 여의도고는 전체 수광점의 약 1/4이라 영향이 크다.

### A− 운동장 격자·경계 도면기준 (140개) — 여의도초 64 · 여의도중 76

#### 여의도중 (6 × 13 = 76점)

분석지점도의 `(i,j)` 라벨 위치를 규칙격자에 맞춰 역산했다. **i 는 동쪽 모서리에서
남서쪽(232°)으로 1~6, j 는 남쪽 모서리에서 북서쪽(322°)으로 1~13** 이다.
격자점마다 라벨 글리프 밀도를 재보니 **(1,13)·(2,13) 두 칸만 비어 있고 나머지
76칸에 분석지점이 있다** — 북쪽 모서리가 잘린 형상이다.

경계는 배치도 해치영역에서 읽었다 — **116.7 × 61.8m · 7,218㎡ · 칸 10.3 × 8.98m**,
중심 E194,265.2 / N547,157.0. 이 사각형을 6×13 으로 나눠 칸마다 배치도 해치
피복률을 재보니 **(1,13) 0% · (2,13) 12%** 로 도면과 정확히 일치했고, 나머지
74칸은 51~100% 였다 — 도면과 배치도가 서로를 검증한다.

종전 8m 격자 근사 208점이 76점이 되면서 **충족률이 크게 달라졌다**:
S1 80.3% → **76.3%**, S2 73.6% → **57.9%**. 근사 영역은 교사동 60m 이내
옥외지반을 다 담아 그림자를 덜 받는 북쪽 여유지까지 세고 있었다.

#### 여의도초 (8 × 8 = 64점)

두 도면을 겹쳐 확정했다.

* **칸 수** — [서울여의도초등학교 옥외체육장 분석지점도]. 점 라벨 62개를 좌표로
  읽어 규칙격자에 맞춘 결과 **8 × 8 = 64칸**, 코너 라벨이 (1,1)·(1,8)·(8,1)·
  (8,8) 로 확인된다. 칸마다 중앙점 1개 — 종전 8m 격자 근사 228개에서 64개.
* **경계** — [학교 위치 배치도]. 이 배치도는 학교마다 색을 칠하고 **운동장을
  해치(격자무늬), 건물을 단색**으로 그린다. 치수·좌표는 없지만 건물 외곽선이
  AL_D010 과 같은 도형이므로 **건물 선화에 맞춰 축척·평행이동을 역산**하면
  (`school_site_map.py`, 축척 **1.270 m/화소**, 선화 일치도 0.511) 해치영역을
  실좌표로 읽을 수 있다. 결과는 **72.6 × 70.9m · 5,146㎡ · 방위 142°**
  (블록 장축 52°의 직교), 중심 E194,374.1 / N547,047.5, 칸 **9.1 × 8.9m**.

래스터 계측이라 경계 정밀도가 **±3m 안팎**이라 A 가 아니라 A− 다.

**종전 위치가 틀렸다.** 경계를 몰라 '옥외지반 최대 내접 사각형'으로 추정했더니
운동장이 교사동 **북동쪽**(2,600㎡)에 놓였는데, 배치도상 실제 운동장은 교사동
**남서쪽**이다. 위치를 바로잡자 여의도초 운동장 충족률이 **98.4% → 78.1%**
(S1)로 내려갔다 — 북동쪽 빈 땅은 그림자가 거의 닿지 않던 곳이다.

### C 운동장 근사 (524개) — 여의도여고·여의도고

실제 운동장 경계가 아니다. 교사동에서 60m 이내 옥외지반 중 ① 어느 건물에도 들지
않고 ② 그 학교가 다른 어떤 건물보다 가까운 8m 격자점만 남겨 배정한 **근사 영역**
이다. `운동장_근사영역` 폴리곤 레이어로 함께 내보냈으니 QGIS 에서 실제 운동장과
대조해 보고, 실제 폴리곤을 그려 주면 `--playground` 로 교체할 수 있다.
두 학교의 **운동장 경계는 이미 같은 배치도에서 읽어 두었다**
(`outputs/school_compliance/playground/운동장_배치도경계_epsg5186.geojson`,
`data/school_playgrounds.json` 의 `_pending`). 각 학교 옥외체육장 분석지점도
(칸 수)를 받으면 여의도초와 같은 방식으로 확정한다.

| 학교 | 배치도 운동장 | 크기 | 중심 |
|---|---:|---|---|
| 여의도고(30-1) | 6,459㎡ | 99.2 × 65.1 m | E194,124.0 / N547,281.8 |
| 여의도여고(40-2) | 4,318㎡ | 54.6 × 79.0 m | E194,245.2 / N547,007.1 |

### A− 도면기준이지만 추론·계측이 들어간 것 (221개)

* **여의도중 운동장 76개 · 여의도초 운동장 64개** — 위 절 참조. 칸 수는
  분석지점도, 경계는 배치도 래스터 계측(**±3m**).
* **여의도중 체육관동 부속(원형부) 48개** — AL_D010 에 용도·층수·높이가 모두
  결측인 787.5㎡ 동이다. 체육관동과 2.3m 거리·합계 면적 일치를 근거로 **같은
  건물의 분할 폴리곤이라고 추론**해 같은 Z그리드와 창 높이를 적용했다.
  높이 15.0m 도 그 추론에 따른 값이다.
* **여의도여고 A동 33개** — AL_D010 이 5층·13.00m 로 층수와 높이가 서로 모순이다
  (5층이면 층고 2.6m). 지침서가 3개층이므로 **3층·층고 4.0m + 파라펫 1.0m 로
  추정**했다. 창 1.5~3.1m 는 창 표기와 층 피치의 비에서 환산해 **±0.2m 오차**가
  있을 수 있다.

### A 안에도 남아 있는 가정

도면기준 441개도 **수평 배치는 교실모듈 9.0m·창 2개소 표준값**을 쓴다. 여의도초·
여의도중 정면도는 연속 리본창이라 창 개소를 셀 수 없기 때문이다. 다만 여의도여고
본관동·A동은 지침서에 점 번호가 있어 **층별 점 개수가 도면과 일치함을 확인**했다.

층고·창높이도 도면 종류에 따라 정밀도가 다르다.

* **치수 실측** — 여의도중(체육관동 4면도에 치수선이 살아 있음)
* **픽셀 계측** — 여의도초·여의도여고(래스터 도면을 화소 단위로 재서 환산)

## QGIS 레이어

`outputs/school_compliance/qgis_provenance/` — 모두 `_epsg5186`(원본)와
`_wgs84`(경위도) 두 벌.

| 레이어 | 내용 | 개수 |
|---|---|---:|
| `수광점_근거등급_전체` | 전량. `grade` 기준 5색 자동 적용 | 1,527 |
| `수광점_추정만` | **B + C — 자료 없이 추정한 것만** | 865 |
| `수광점_추정_일반식` | B 만 | 341 |
| `수광점_추정_운동장근사` | C(+) 만 | 524 |
| `수광점_도면기준_추론포함` | A− 만 | 221 |
| `운동장_근사영역` | 실제로 평가에 쓴 운동장 폴리곤(여의도초는 배치도 경계) | 4 |

별도로 `outputs/school_compliance/playground/` 에 배치도에서 읽은
**4개교 운동장 경계**(`운동장_배치도경계_epsg5186.geojson`)와 확인도를 냈다.

속성: `pid` · `school` · `kind` · `dong` · `floor` · `z_m` · `grade`(A/A−/B/C+/C) ·
`근거등급` · `근거` · `추정항목`(무엇을 추정했는지 문장) · `estimated`(참/거짓)

`.qml` 이 함께 있어 QGIS 에서 열면 등급별 색이 자동 적용된다 —
B 빨강(큰 점) · A− 주황 · C+ 노랑 · C 회색(작은 점) · A 청록.
표 형식은 `수광점_근거등급.csv` 로도 냈다.

## 보완 진행 순서

사용자 지정 순서 — **1. 여의도초 → 2. 여의도중 → 3. 여의도여고 → 4. 여의도고**,
B·C 등급이 대상이다.

| 순서 | 학교 | 남은 B·C | 필요한 자료 |
|---|---|---:|---|
| 1 | 여의도초 | **없음** (운동장 A−) | 끝. 실측 좌표를 주면 A |
| 2 | 여의도중 | **없음** (운동장 A−) | 끝. 실측 좌표를 주면 A |
| 3 | 여의도여고 | ①·②동 95(B), 운동장 141(C) | ①·②동 정면도, 옥외체육장 **분석지점도(칸 수)** — 경계는 확보 |
| 4 | 여의도고 | 교사동 246(B), 운동장 383(C) | 교사동 정면도, 옥외체육장 **분석지점도(칸 수)** — 경계는 확보 |
